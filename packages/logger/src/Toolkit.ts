/**
 * Re-exports evlog's toolkit — the building blocks used to integrate
 * request-scoped wide-event logging into a framework.
 *
 * Available as `@vercube/logger/toolkit`.
 *
 * @see https://evlog.dev
 */
export * from 'evlog/toolkit';
