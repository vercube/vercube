name: Release Commit

permissions: {}

on:
  pull_request:
    branches: [main, v1]

jobs:
  release-commit:
    permissions: {}
    uses: sxzz/workflows/.github/workflows/release-commit.yml@v1
    with:
      build: pnpm build
      packages: "./packages/*"
      comment-package-manager: pnpm