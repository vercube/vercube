name: Coverage

on:
  push:
    branches: [main, v1]
  pull_request:
    branches: [main, v1]

jobs:
  coverage:
    uses: sxzz/workflows/.github/workflows/coverage.yml@v1
    permissions:
      id-token: write