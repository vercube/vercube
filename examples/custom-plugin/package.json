{
  "name": "@examples/custom-plugin",
  "version": "1.0.0",
  "description": "Example: vercube.config plugins (configure, setup, setupCLI)",
  "packageManager": "pnpm@10.33.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vercube build",
    "dev": "vercube dev",
    "plugin-info": "vercube plugin-info",
    "tscheck": "tsgo --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@oxc-project/runtime": "catalog:",
    "@vercube/cli": "workspace:*",
    "@vercube/core": "workspace:*",
    "@vercube/di": "workspace:*",
    "@vercube/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.3.11"
  }
}
