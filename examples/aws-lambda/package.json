{
  "name": "@examples/aws-lambda",
  "version": "1.0.0",
  "description": "Base example for Vercube",
  "packageManager": "pnpm@11.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vercube build",
    "dev": "vercube dev",
    "prestart": "vercube build",
    "start": "serverless offline --stage local --noPrependStageInUrl --host 0.0.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@oxc-project/runtime": "catalog:",
    "@vercube/cli": "workspace:*",
    "@vercube/core": "workspace:*",
    "@vercube/di": "workspace:*",
    "@vercube/logger": "workspace:*",
    "@vercube/serverless": "workspace:*",
    "serverless": "4.38.1",
    "serverless-offline": "14.7.4"
  }
}
